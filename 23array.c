//Program to insert an element in an array
#include<stdio.h>
void main()
{
    int n,a[n],i;                        //n is the size of the array
    printf("size of array:");
    scanf("%d",&n);
    printf("input element:");
    for(i=0;i<=n;i++)
    {
    scanf("%d",&a[n]);
    }
}