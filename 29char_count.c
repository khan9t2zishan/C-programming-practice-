//program to count words in string
#include<stdio.h>
#include<string.h>
int main()
{
    char arr[100];
    printf("Enter the string: "); 
    gets(arr);
    puts(arr);
    strlen(arr);
    printf("\nLength of the string is: %d",strlen(arr));
    return 0;
}
